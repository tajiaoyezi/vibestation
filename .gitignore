# ────────────── OS ──────────────
.DS_Store
._*
Thumbs.db
Desktop.ini

# ────────────── Editor / IDE ──────────────
.vscode/
.idea/
*.swp
*.swo
*~
.zed/

# ────────────── Rust / Cargo ──────────────
target/
# Cargo.lock 进 git：Vibestation 是 binary application（非 library）· Rust 官方
# 建议 binary crate 提交 lockfile · 保证依赖树跨 clone / 跨 CI run 完全一致。
# Spike benchmark 归档同样需要冻结依赖（docs/spikes/code/**/Cargo.lock 也进 git）。
**/*.rs.bk
*.pdb

# ────────────── Node / npm / pnpm ──────────────
node_modules/
.pnp
.pnp.js
.yarn-integrity
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# TypeScript 增量 build 产物（本地 tsc -b 历史残留）
web/tsconfig.tsbuildinfo
# tsc 误 emit 的 JSX（防 noEmit 配置被意外改回 · 新 build 配置 tsc --noEmit）
web/src/**/*.jsx
web/src/**/*.js

# ────────────── Tauri ──────────────
src-tauri/target/
src-tauri/gen/
src-tauri/WixTools/
# MVP-01 实际 Tauri app 位于 crates/app/（CLAUDE.md 决策表 #5 · 2 crate workspace）
crates/app/target/
crates/app/gen/
crates/app/WixTools/
*.app
*.dmg
*.AppImage
*.deb
*.msi
*.exe

# ────────────── Frontend build ──────────────
dist/
dist-ssr/
build/
.vite/
.turbo/
.next/
.nuxt/
.output/
.astro/
.svelte-kit/

# ────────────── Env / secrets ──────────────
.env
.env.local
.env.*.local
.secrets
secrets.toml

# ────────────── Logs / caches ──────────────
*.log
logs/
.cache/
.parcel-cache/
.eslintcache
.stylelintcache

# ────────────── Test / coverage ──────────────
coverage/
*.lcov
.nyc_output/
test-results/
playwright-report/

# ────────────── Local dev databases / data ──────────────
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.redb
*.redb-lock
dev-data/
user-data/

# ────────────── Package manager ──────────────
.npm/
.yarn/
.bun/
bun.lockb

# ────────────── Vibestation 特定 ──────────────
# 本地 Spike / 实验目录
spike-tmp/
# Spike 录屏 / 截图 / 大文件产物（本地归档 · 不入 repo · 走 LFS 或外部存储）
# 背景：SPIKE-01 macos-ime.mov 2.4MB 意外进 commit · 此规则防复发
spike-artifacts/
# Spike 原始未脱敏捕获（SPIKE-06 CLI 输出样本 · 含 auth token / PII · 严禁进 repo）
# 任何以 .raw 结尾的文件、任何 spike-raw 目录均被屏蔽
# 这条规则与 SPIKE-06 §A.5.1 的文件命名约定配套（强制 .raw 后缀）
# 注意：`~/.vibestation-spike-raw/` 在 repo worktree 外，gitignore 无法覆盖
#       ——依赖命名约定 + Phase 4 CI 的 gitleaks 双层防护
*.raw
spike-raw/
.spike-raw/
# 本地 profiling 产物
*.pprof
*.heapprofile
flamegraph.svg
# 本地终端配置扫描缓存
.vs-profile-cache/
.omx/
