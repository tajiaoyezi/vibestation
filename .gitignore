# ────────────── OS ──────────────
.DS_Store
._*
Thumbs.db
Desktop.ini

# ────────────── Editor / IDE ──────────────
.vscode/
.idea/
*.swp
*.swo
*~
.zed/

# ────────────── Rust / Cargo ──────────────
target/
Cargo.lock
**/*.rs.bk
*.pdb

# ────────────── Node / npm / pnpm ──────────────
node_modules/
.pnp
.pnp.js
.yarn-integrity
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ────────────── Tauri ──────────────
src-tauri/target/
src-tauri/gen/
src-tauri/WixTools/
*.app
*.dmg
*.AppImage
*.deb
*.msi
*.exe

# ────────────── Frontend build ──────────────
dist/
dist-ssr/
build/
.vite/
.turbo/
.next/
.nuxt/
.output/
.astro/
.svelte-kit/

# ────────────── Env / secrets ──────────────
.env
.env.local
.env.*.local
.secrets
secrets.toml

# ────────────── Logs / caches ──────────────
*.log
logs/
.cache/
.parcel-cache/
.eslintcache
.stylelintcache

# ────────────── Test / coverage ──────────────
coverage/
*.lcov
.nyc_output/
test-results/
playwright-report/

# ────────────── Local dev databases / data ──────────────
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.redb
*.redb-lock
dev-data/
user-data/

# ────────────── Package manager ──────────────
.npm/
.yarn/
.bun/
bun.lockb

# ────────────── Vibestation 特定 ──────────────
# 本地 Spike / 实验目录
spike-tmp/
# Spike 原始未脱敏捕获（SPIKE-06 CLI 输出样本 · 含 auth token / PII · 严禁进 repo）
# 任何以 .raw 结尾的文件、任何 spike-raw 目录均被屏蔽
# 这条规则与 SPIKE-06 §A.5.1 的文件命名约定配套（强制 .raw 后缀）
# 注意：`~/.vibestation-spike-raw/` 在 repo worktree 外，gitignore 无法覆盖
#       ——依赖命名约定 + Phase 4 CI 的 gitleaks 双层防护
*.raw
spike-raw/
.spike-raw/
# 本地 profiling 产物
*.pprof
*.heapprofile
flamegraph.svg
# 本地终端配置扫描缓存
.vs-profile-cache/
.omx/
