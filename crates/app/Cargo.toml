[package]
name = "vibestation-app"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Vibestation Tauri 2 启动层（app crate · MVP-02 workspace management）"

[lib]
name = "vibestation_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }
ts-rs.workspace = true
vibestation-core = { path = "../core" }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-dialog = "2"
tauri-plugin-clipboard-manager = "2"
serde.workspace = true
serde_json.workspace = true
vibestation-core = { path = "../core" }
r2d2.workspace = true
r2d2_sqlite.workspace = true
chrono.workspace = true

[dev-dependencies]
tempfile.workspace = true
git2 = "0.20"
# MVP-04 §I shell 兼容矩阵 integration test · 走 PTY runtime 事件 · 与 core crate 同版本
crossbeam-channel = "0.5"
