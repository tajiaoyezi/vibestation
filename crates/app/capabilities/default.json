{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Vibestation Phase A 默认窗口 capability · 仅含启动所需最小权限 · Phase B 将收紧 core:default 到具体子集",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-greet"
  ]
}
