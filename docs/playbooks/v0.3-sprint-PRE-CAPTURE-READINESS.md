# v0.3 Sprint Phase D · Batch Pre-Capture 就绪体检

> **定位**：本文件是 [`v0.3-sprint-capture-playbook.md`](./v0.3-sprint-capture-playbook.md)（Arbiter 90-120 min 一气呵成跑完 4 MVP）的**前置体检**——主 agent（CLI）能程序化验证的 4 MVP（MVP-12 / MVP-14 / MVP-15 / MVP-16）代码侧前置已全跑，结论固化在此。
> **它不是 capture 本身**：playbook 的 27 张 evidence + DevTools Performance + Lighthouse WCAG audit + 跨平台 smoke 设计上就是 Arbiter 本人完成 · CLI agent 无 webview / 无 Performance flamegraph / 无 Lighthouse 能力。
> **用途**：Arbiter 跑 v0.3 sprint Phase D batch capture 窗口时（90-120 min）· 先读本文件 —— 4 MVP 代码侧已 green 的部分不必重复验；聚焦真正需要人的 GUI / DevTools / 视觉回归 / a11y。
>
> 体检执行：Claude Code 主 agent · 2026-05-20 · 镜像 MVP-19 #384 模式

---

## ✅ 已自动验证就绪（Arbiter 实跑可信任 · 不必重复验）

| MVP                           | 代码侧 verify                                                                               | 结果                                                                                                                                       |
| ----------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| MVP-12                        | 纯前端 RailGraph（`web/src/panels/GitLog/RailGraph/`）+ 后端 `rail_graph_events.rs`         | Phase A/B/C 代码 done · `ENABLE_RAIL_GRAPH` flag 默认 false（capture 时临时翻 true）· 待 Arbiter capture 后翻 flag 默认 true               |
| MVP-14                        | 纯前端 Pane 高级布局（`web/src/panels/Terminal/PaneSplitView` 等 · 复用 MVP-05 panes 基础） | Phase A/B/C 代码 done · panes::tests:: 63 + pane_service::tests:: 23 = 86 passed（继承 MVP-05 verify）                                     |
| MVP-15                        | 纯前端 shiki lazy load · 后端无新代码                                                       | Phase A/B/C + D 自动化子项 done · vitest DiffLine 渲染断言 3 case passed（shiki@4.1.0 兼容 · PR #399 已 verify）                           |
| MVP-16                        | `cargo test -p vibestation-core --lib 'rebase_ops::tests'`                                  | **54 passed · 0 failed · 0 ignored**（含 rebase / merge / cherry-pick 全链 + 3-way conflict + crash recovery）                             |
| **diff（MVP-15 dependency）** | `cargo test -p vibestation-core --lib 'diff::tests'`                                        | **10 passed · 0 failed**（含 binary_detection_beats_large_file_truncation + binary_files_short_circuit + get_view_mode_defaults_to_split） |

### v0.3 sprint playbook 14 invariant 已写入（不重复列）

playbook §14 Invariant 引用 MVP-05 playbook 14 invariant · self-check grep 已编写：

```bash
grep -nE "测 1 次|状态可选|optional|大致" docs/playbooks/v0.3-sprint-capture-playbook.md   # I2/I4
grep -nE "<TBD>|<yes_or_no>|<PASS_or_FAIL>" docs/playbooks/v0.3-sprint-capture-playbook.md   # I9 占位符
grep -nE "判定：FAIL|判定：no" docs/runtime-evidence/mvp-1[2-6]/metrics-*.md 2>/dev/null   # I12 显式 FAIL 拒绝
```

Arbiter 实跑后 self-check grep 验证 invariant。

---

## ⚠️ 关键 gap 预警

### gap-1 · 4 MVP Phase D 27 张 evidence + DevTools + 视觉回归 + WCAG 全空

**坐实**：`ls docs/runtime-evidence/mvp-{12,14,15,16}/phase-d/` = 4 目录均不存在或为空。

**影响**：playbook §1-§4 预期产出（MVP-12 8 evidence + MVP-14 4 PNG + 1 录屏 + 5 DevTools 数字 + MVP-15 10 baseline screenshot + 4 DevTools + Lighthouse WCAG + MVP-16 4 PNG · Linux 跨平台 deferred v0.2 W17 dev VM）· 当前 0/27 evidence 到位。

**不是 gap 是 deferred**：spec / playbook 明确设计为 Arbiter 90-120 min batch capture 窗口（v0.1 GA gate 后 / v0.2 准备阶段 / v0.3 sprint 收口节点触发）。需 Arbiter 启 Phase D capture 窗口。

### gap-2 · MVP-12 ENABLE_RAIL_GRAPH flag 默认 false

**坐实**：`grep 'ENABLE_RAIL_GRAPH' web/src/panels/GitLog/RailGraph/types.ts` = `export const ENABLE_RAIL_GRAPH = false;`（默认 false · 用户不可见）。

**影响**：playbook §1.1 设计为 capture 时临时 `sed` 翻 true · capture 完 git restore。意图：rail graph 是 v0.3 实验性 feature · capture 完后是否翻 default true 由 Arbiter v0.3 ship 决策（playbook 已注「不要 commit 这个改动」）。

### gap-3 · MVP-12 1M commit fixture 可能缺

**坐实**：playbook §1.2 提到「1M fixture 若不存在 · Arbiter 用脚本生成 · 时间 5 min」· 当前未 verify 1M fixture 是否在 `web/tests/fixtures/rail-graph/` 中。capture 前 Arbiter 需要 5 min 生成。

---

## 🔴 Arbiter 实跑必须本人完成（CLI agent 无法替代 · 不得编造）

按 [`v0.3-sprint-capture-playbook.md`](./v0.3-sprint-capture-playbook.md) §0-§6：

### §0 通用 setup（5 min · 一次性）

- `git checkout main && git pull && git status clean`
- `pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint`
- mkdir 4 个 `docs/runtime-evidence/mvp-{12,14,15,16}/phase-d`
- `lsof -iTCP:1420` 验 dev server 无残留

### §1 MVP-12 capture（25-35 min · 8 evidence + 4 metrics）

- 翻 ENABLE_RAIL_GRAPH flag = true（临时 · 不 commit）
- 准备 fixture 10k / 100k / 1M commit
- 8 张 PNG（rail graph first paint / 100k fixture / hover highlight / 21-50 lane collapse / >50 lane Other branches / 100k 30s memory / 1M no crash / 10s fps trace）
- 4 DevTools Performance 数字（F.1 首屏 / F.5 CPU / F.6 内存 / F.2 滚动 FPS）
- 1M commit 不崩验证（允许 collapse 降级）
- 跑完 `git restore web/src/panels/GitLog/RailGraph/types.ts`

### §2 MVP-14 capture（15-20 min · 4 PNG + 1 录屏 + 5 DevTools）

- 4 张 PNG（Dual AI / Triple Review / Quad / maximize）
- 1 段 10s 录屏（pane navigation 全键盘流程）
- 5 DevTools 数字（§H.1-H.5 各 P99 · 复用 MVP-05 panes 仪表化）

### §3 MVP-15 capture（30-40 min · 10 baseline + 4 DevTools + Lighthouse）

- 10 张 baseline screenshot（5 lang × 2 theme · light/dark）
- 4 DevTools Performance（F.1/F.2/F.3/F.5）· shiki worker lazy load 验证
- §E.2 Lighthouse WCAG AA audit（webview 在 DevTools Lighthouse 跑 · 验 a11y score ≥ 95）
- 截 baseline 后下次 PR 用 visual regression diff（playwright screenshot match）

### §4 MVP-16 capture（10-15 min · 4 PNG）

- 4 张 PNG（RebaseEditor / 3-way conflict / ConflictBanner / CrashBanner）
- Linux 跨平台 deferred v0.2 W17 dev VM（spec 明确）

### §5 收尾

- `npx prettier --write docs/runtime-evidence/mvp-1[2-6]/**/*.md`
- `bash scripts/validate-runtime-evidence.mjs`（playbook 提到 Cursor track 3 交付 · 待 PR）
- 27 evidence + 4 metrics 文件全归档
- commit + PR + R1-R5 4 MVP 同 PR 翻 done

---

## §6 Arbiter capture 后翻转 4 MVP spec frontmatter

按 playbook §6 commit checklist · 实际 Arbiter 跑完 + main 上有 27 evidence 后 · 主 agent（CLI）才能：

1. 跑 `bash scripts/validate-runtime-evidence.mjs` 验 27 evidence 全到位 + metrics 全填
2. `docs/tasks/MVP-{12,14,15,16}-*.md` frontmatter `status: ready` → `status: done`
3. 同步 PROGRESS.md + CLAUDE.md + docs/tasks/README.md

---

## 结论

v0.3 sprint Phase D batch 验收项中：

- **代码侧 4 MVP 全 verify 绿**（MVP-12 后端 rail_graph_events + 前端 RailGraph done · MVP-14 复用 MVP-05 panes 86 passed · MVP-15 shiki@4.1.0 兼容 + diff 10 passed · MVP-16 rebase_ops 54 passed · 共 86+10+54+继承 = 150+ tests · 0 failed）✅
- **27 evidence + DevTools + Lighthouse + 1M commit fixture 全待 Arbiter**（90-120 min batch capture · 必须人工 GUI 实跑）🔴
- **MVP-12 ENABLE_RAIL_GRAPH flag 默认 false**（capture 时临时翻 true · 翻 default true 是 v0.3 ship 决策）🟡
- **MVP-12 1M commit fixture 可能缺**（capture 前 Arbiter 5 min 用脚本生成）🟡
- **Linux 跨平台 deferred v0.2 W17 dev VM**（spec 明确）🟡

4 MVP spec 维持 `ready` / `in-progress`（代码 done · capture batch 待 Arbiter playbook 窗口）。

**关联**：[`v0.3-sprint-capture-playbook.md`](./v0.3-sprint-capture-playbook.md) §0-§6 · 4 spec [`MVP-12`](../tasks/MVP-12-commit-rail-graph.md) / [`MVP-14`](../tasks/MVP-14-pane-advanced-layout.md) / [`MVP-15`](../tasks/MVP-15-diff-syntax-highlight.md) / [`MVP-16`](../tasks/MVP-16-rebase-merge-cherrypick.md) · `.claude/rules/runtime-evidence-location.md` R1-R5 · 模板镜像 [MVP-19 PRE-CAPTURE-READINESS](../runtime-evidence/mvp-19/PRE-CAPTURE-READINESS.md) PR #384
