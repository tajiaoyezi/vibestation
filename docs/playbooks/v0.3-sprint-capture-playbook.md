# v0.3 Sprint Phase D · Capture Playbook · Arbiter 90-120 min 一气呵成

> **目标**：按本 playbook 一次跑完 4 个 v0.3 sprint MVP 的 Phase D GUI / DevTools / 视觉回归 evidence · 让主 agent 据此把 MVP-12 / MVP-14 / MVP-15 / MVP-16 spec frontmatter 从 `ready` 翻 `done`。
>
> **触发条件**：v0.1 GA gate 后 · v0.2 准备阶段（或 v0.3 sprint 收口节点）· 4 MVP 的 Phase A+B+C 代码已 done · 仅剩 Phase D GUI + DevTools 数字。
>
> **总评估时间**：90-120 min（含 5 min 通用 setup + 4 MVP × 20-30 min 实测 + 10 min 汇总 commit）
>
> **预期产出**：
> - MVP-12：6-8 张 PNG/MP4 · 4 DevTools Performance 数字（F.1/F.4/F.6 + 1M commit 不崩验证）+ §H 视觉锁定记录
> - MVP-14：4 张 PNG（Dual AI / Triple Review / Quad / maximize）+ 1 段 10s 录屏 + §H.1-H.5 5 个 DevTools 数字
> - MVP-15：10 张 baseline screenshot（5 lang × 2 theme）+ 4 DevTools Performance 数字（F.1/F.2/F.3/F.5）+ §E.2 Lighthouse WCAG AA audit
> - MVP-16：4 张 PNG（RebaseEditor / 3-way conflict / ConflictBanner / CrashBanner）· Linux 跨平台 deferred v0.2 W17 dev VM
>
> **关联**：
> - MVP-05 playbook 参考：[`docs/runtime-evidence/mvp-05/CAPTURE-PLAYBOOK.md`](../runtime-evidence/mvp-05/CAPTURE-PLAYBOOK.md)（14 invariant 详述 · 本 playbook 引用不重复）
> - rule：[`.claude/rules/runtime-evidence-location.md`](../../.claude/rules/runtime-evidence-location.md) R1-R5
> - validator：`scripts/validate-runtime-evidence.mjs`（待 PR · Cursor track 3 交付）

---

## 🛡 14 Invariant · 引用 MVP-05 playbook

本 playbook 设计依据与 MVP-05 playbook 相同 14 invariant（Codex round 1-4 review fix · 2026-05-03 实战验证）· 详见 [MVP-05 playbook §14 Invariant](../runtime-evidence/mvp-05/CAPTURE-PLAYBOOK.md#-14-invariantplaybook-design-intent--codex-round-1234-review-fix--2026-05-03)。

**自检 grep**（写完 / 改完 playbook 任何段后必跑 · 任一异常违反对应 invariant）：

```bash
# I2/I4 自检（含糊步骤 / 可选 mandatory · 真 mandatory 段不能出现）
grep -nE "测 1 次|状态可选|optional|大致" docs/playbooks/v0.3-sprint-capture-playbook.md

# I5 自检（跨段引用一致 · 4 MVP evidence 数量在 §1-§4 头表 vs §6 commit checklist 必须一致）
# 手工 cross-check：§1.0 表 vs §6 checklist · evidence count 必须相等

# I9 自检（validator 不匹配 template token · placeholder 用 <TBD> 风格）
grep -nE "<TBD>|<yes_or_no>|<PASS_or_FAIL>" docs/playbooks/v0.3-sprint-capture-playbook.md
# 上面输出的就是 runner 必填项 · 不填 grep 不通过 → BLOCK commit

# I12 自检（validator 拒绝 explicit FAIL · 任一 FAIL 即 BLOCK）
grep -nE "判定：FAIL|判定：no" docs/runtime-evidence/mvp-1[2-6]/metrics-*.md 2>/dev/null
# 上面有任何输出 · runner 必须 fix 后才能 commit · 主 agent 不应接 FAIL PR
```

---

## 🎬 §0 · 通用环境 setup（5 min · 一次性）

### 0.1 仓库就绪

```bash
cd /Users/leaf/CodeWorkSpace/PersonalWorkspace/vibestation
git checkout main
git pull origin main
git status   # 必须 clean · 否则 stash 或 commit
git log -1 --oneline   # 记录 baseline HEAD（写到 metrics 文件）
```

### 0.2 依赖就绪

```bash
pnpm install --frozen-lockfile
pnpm typecheck   # 0 errors
pnpm lint        # All matched files use Prettier code style!
```

### 0.3 evidence 目录就绪

```bash
mkdir -p docs/runtime-evidence/mvp-12/phase-d
mkdir -p docs/runtime-evidence/mvp-14/phase-d
mkdir -p docs/runtime-evidence/mvp-15/phase-d
mkdir -p docs/runtime-evidence/mvp-16/phase-d
```

### 0.4 录屏 / 截图工具

- **macOS**：Screenshot.app（⌘⇧5）/ QuickTime Player 屏幕录制 / Cleanshot X
- **录屏统一规格**：1280×800 viewport · 10-30s · MP4 H.264 · ≤ 5MB（按 [runtime-evidence-location.md](../../.claude/rules/runtime-evidence-location.md) R4）
- **截图统一规格**：1280×800 viewport · PNG · ≤ 500KB（推荐）· ≤ 3MB（上限）

### 0.5 Tauri dev mode 启动验证（确认无残留 dev server）

```bash
lsof -iTCP:1420 -sTCP:LISTEN   # 必须空 · 否则 pkill -f "vite\|tauri"
pnpm tauri:dev                  # 等窗口 ready · ~5s
# 验证后 Ctrl+C 关闭 · 每个 MVP capture 时再启
```

---

## §1 · MVP-12 Phase D capture（25-35 min · 8 evidence + 4 metrics）

### 1.0 evidence 清单总览（mandatory · I5 跨段一致）

| # | 文件 | acceptance | 用途 |
|---|---|---|---|
| 01 | `01-rail-graph-first-paint.png` | F.1（10k commit 首屏 < 500ms） | DevTools Performance 截图 · 含 Network/Main 时间轴 |
| 02 | `02-rail-graph-100k-fixture.png` | F.5（1 万 commit CPU < 单核 40%） | 100k commit fixture 滚动中 |
| 03 | `03-hover-rail-highlight.png` | C.4/C.5（hover rail path 高亮） | 节点 hover 后高亮 rail 截图 |
| 04 | `04-collapse-21-50-lane.png` | C.6（21-50 lane 压缩到 8px + tooltip） | 30 lane fixture |
| 05 | `05-collapse-over-50-other-branches.png` | C.7（>50 lane Other branches dropdown） | 80 lane fixture · 展开 dropdown |
| 06 | `06-100k-commit-30s-memory.png` | F.6（100k commit 30s 稳态内存增量 < 120MB） | Chrome DevTools Memory snapshot |
| 07 | `07-1m-commit-no-crash.png` | F.7（100 万 commit 不崩 · 允许 collapse 降级） | 1M commit fixture · 截 collapse 后状态 |
| 08 | `08-scroll-10s-fps-trace.png` | F.2（滚动 16ms / 60FPS P99） | DevTools Performance 滚动 trace |

### 1.1 翻 ENABLE_RAIL_GRAPH flag（临时 · 不 commit）

```bash
# 本地编辑 web/src/panels/GitLog/RailGraph/types.ts
# 找 `export const ENABLE_RAIL_GRAPH = false;` · 改成 true
# 注意：不要 commit 这个改动 · capture 完后 git restore
sed -i.bak 's/ENABLE_RAIL_GRAPH = false/ENABLE_RAIL_GRAPH = true/' web/src/panels/GitLog/RailGraph/types.ts
```

### 1.2 启动 dev mode · 准备 fixture

```bash
pnpm tauri:dev   # 后台 · 等窗口 ready
```

- 在 Vibestation 内开一个 git workspace · 注入 fixture 10k commit / 100k commit / 1M commit（用 `web/tests/fixtures/rail-graph/{linear_20,branchy_1k,kernel_like_100k}.json` · 1M fixture 若不存在 · Arbiter 用脚本生成 · 时间 5 min）

### 1.3 evidence 01 · 首屏 + F.1 数字（< 500ms P99）

1. DevTools 打开 Performance 面板 · 准备录制
2. 切到 git log panel（含 10k commit fixture）· **开始录制前清空 panel · refresh** · 触发首屏渲染
3. 录制 5s · 标记 `mvp-12.draw.start` / `mvp-12.draw.end`
4. **测 5 次取 P99** · 在 metrics 文件填 `F.1 首屏 P99：<TBD>ms · 判定：<PASS_or_FAIL>`
5. 截图 1280×800 viewport 含 DevTools Performance 面板 → `docs/runtime-evidence/mvp-12/phase-d/01-rail-graph-first-paint.png`

### 1.4 evidence 02 · 100k commit fixture 滚动 + F.5 数字

1. 切到 100k commit fixture
2. DevTools Performance 录制 10s · 持续滚动（鼠标滚轮快速滚 · 模拟用户浏览）
3. Performance 面板看 CPU 占用（顶部曲线）· P99 < 单核 40%（单核基准看本机 CPU · M2 Max = 25% 总占用 = 单核 40% × 1/(N核数) · M2 Max 12 核 → 总占用 ~3.3% 即等效单核 40%）
4. metrics 填 `F.5 CPU P99：<TBD>% 总 · 判定：<PASS_or_FAIL>`
5. 截图 → `02-rail-graph-100k-fixture.png`

### 1.5 evidence 03 · hover rail path 高亮

1. 100k fixture 内随机找一个 merge commit
2. hover 节点 · 等 1 帧 · 截图看 rail path 全条高亮（连接到该 commit 的整条 lane）
3. 验证 C.4 acceptance：hover 节点 1 帧内显示完整 rail 高亮
4. 截图 → `03-hover-rail-highlight.png`

### 1.6 evidence 04 · 21-50 lane 压缩 8px + tooltip

1. 用 30 lane fixture（若不存在 · Arbiter 用 `scripts/fixtures/` 内的 generator 生成 · 或 git history 找 30 branch 仓库）
2. 截图看 rail 压缩到 8px · hover 节点显示 tooltip "压缩模式 · hover 查看分支名"
3. 验证 C.6 acceptance
4. 截图 → `04-collapse-21-50-lane.png`

### 1.7 evidence 05 · >50 lane Other branches dropdown

1. 用 80 lane fixture · 截图看前 20 lane 正常显示 · 21+ 合并为 "Other branches" 收纳行
2. 点击 dropdown 展开 · 截图看具体分支列表
3. 验证 C.7 acceptance
4. 截图 → `05-collapse-over-50-other-branches.png`

### 1.8 evidence 06 · 100k commit 30s 稳态内存（F.6）

1. Chrome DevTools Memory 面板 · 准备 heap snapshot
2. 切 100k fixture · 等 30s 稳态（不滚动）· 取 snapshot
3. 对比初始 baseline snapshot（启动后无 rail graph 时）· 计算 delta
4. metrics 填 `F.6 内存增量：<TBD>MB · 判定：<PASS_or_FAIL>`（目标 < 120MB）
5. 截图 → `06-100k-commit-30s-memory.png`

### 1.9 evidence 07 · 1M commit 不崩（F.7）

1. 切到 1M commit fixture · 等 collapse 降级触发
2. 验证 acceptance：不 crash · 允许降级到 collapse + 简化线条
3. 截图看 collapse 后的简化视图 → `07-1m-commit-no-crash.png`
4. metrics 填 `F.7 1M commit：<degraded_or_full> · 判定：<PASS_or_FAIL>`

### 1.10 evidence 08 · 60FPS 滚动 trace（F.2）

1. DevTools Performance 录制 5s · 持续滚动
2. 看 frame timeline · P99 frame time < 16ms
3. metrics 填 `F.2 滚动 P99：<TBD>ms · 判定：<PASS_or_FAIL>`
4. 截图 → `08-scroll-10s-fps-trace.png`

### 1.11 收尾 · 关 dev mode + 还原 flag

```bash
# Ctrl+C 关 dev mode
pkill -f "vite\|tauri"
lsof -iTCP:1420   # 必须空

# 还原 ENABLE_RAIL_GRAPH=false
mv web/src/panels/GitLog/RailGraph/types.ts.bak web/src/panels/GitLog/RailGraph/types.ts
# 或 git restore web/src/panels/GitLog/RailGraph/types.ts
git diff web/src/panels/GitLog/RailGraph/types.ts   # 必须空
```

### 1.12 metrics + README

写入 `docs/runtime-evidence/mvp-12/phase-d/metrics.md`：

```markdown
# MVP-12 Phase D · metrics

- baseline HEAD: <git rev-parse HEAD>
- capture 时间: <YYYY-MM-DDTHH:MM:SS>
- 平台: macOS 26.3.1 · Apple M2 Max arm64
- ENABLE_RAIL_GRAPH 临时翻 true · capture 完已还原 false（git diff 验证）

## acceptance 判定

- F.1 10k commit 首屏 P99：<TBD>ms · 判定：<PASS_or_FAIL>
- F.2 滚动帧 P99：<TBD>ms · 判定：<PASS_or_FAIL>
- F.5 1 万 commit CPU 总占用：<TBD>% · 判定：<PASS_or_FAIL>
- F.6 100k commit 30s 内存增量：<TBD>MB · 判定：<PASS_or_FAIL>
- F.7 1M commit：<degraded_or_full> · 判定：<PASS_or_FAIL>
- C.4 hover rail 高亮：<yes_or_no> · 判定：<PASS_or_FAIL>
- C.6 21-50 lane 压缩 + tooltip：<yes_or_no> · 判定：<PASS_or_FAIL>
- C.7 >50 lane Other branches dropdown：<yes_or_no> · 判定：<PASS_or_FAIL>
```

---

## §2 · MVP-14 Phase D capture（20-25 min · 5 evidence + 5 metrics）

### 2.0 evidence 清单总览

| # | 文件 | acceptance | 用途 |
|---|---|---|---|
| 01 | `01-preset-dual-ai.png` | B.2（Dual AI 等价 `H(0.5, ClaudeCli, CodexCli)`） | preset 截图 |
| 02 | `02-preset-triple-review.png` | B.3（Triple Review 等价 `H(0.5, ClaudeCli, V(0.5, Runner, Log))`） | preset 截图 |
| 03 | `03-preset-quad.png` | B.4（Quad 等价 `H(0.5, V(0.5, A, B), V(0.5, C, D))`） | preset 截图 |
| 04 | `04-cmd-enter-maximize.png` | E.1-E.5（⌘Enter 最大化 / 恢复 / 动画 < 200ms / state cleanup） | maximize 状态截图 |
| 05 | `05-drag-navigation-10s.mp4` | D.1-D.6 + C.3-C.6 + ratio drag 综合 | 10s 录屏 |

### 2.1 准备 + 启动

```bash
pnpm tauri:dev
# 等窗口 ready · 在 Vibestation 内开一个 workspace · 默认 Solo pane
```

### 2.2 evidence 01 · Dual AI preset

1. 在 Smart Layouts menu 选 `Dual AI`
2. 验证应用后 layout：左 Claude CLI · 右 Codex CLI · ratio 50/50
3. 验证 PTY session id 复用（若已有 Claude/Codex pane · 不重连）
4. 截图 1280×800 · 显示完整两 pane · 标题栏写 "Dual AI" → `01-preset-dual-ai.png`

### 2.3 evidence 02 · Triple Review

1. 切到 `Triple Review`
2. 验证 layout：左 Claude CLI · 右上 Runner · 右下 Log · 右侧两 splitter 可独立拖
3. 截图 → `02-preset-triple-review.png`

### 2.4 evidence 03 · Quad

1. 切到 `Quad`
2. 验证 layout：H(V(A,B), V(C,D)) · 4 pane 等分
3. 截图 → `03-preset-quad.png`

### 2.5 evidence 04 · ⌘Enter maximize + 动画 < 200ms

1. 在 Quad 状态 · focus 任一 pane（如 A）
2. DevTools Performance 录制 5s · 按 ⌘Enter · 等动画完成 · 再按 ⌘Enter 恢复
3. Performance 面板看 maximize transition < 200ms（E.3）
4. 截图 maximize 状态（A pane 全屏 · 其他 hidden · maximized chip 显示）→ `04-cmd-enter-maximize.png`
5. metrics 填 `E.3 maximize 动画 P99：<TBD>ms · 判定：<PASS_or_FAIL>`

### 2.6 evidence 05 · 10s drag + navigation 录屏

录屏 10s · 含以下场景（连续录制 · 不切镜）：

1. ratio drag · 任一 splitter 拖 1s（D.3 / C.3 / C.6）
2. ⌘⌥Right · navigate 到右 pane（D.1 / D.2 / D.4）
3. ⌘⌥Down · navigate（若有目标）/ no-op flash（D.4）
4. ⌘Enter maximize · 1s · ⌘Enter 恢复（E.1 / E.2）

录屏文件 → `05-drag-navigation-10s.mp4` · 体积 ≤ 5MB（HandBrake 压缩）

### 2.7 §H.1-H.5 metrics（DevTools Performance · 仅 1 次取数 · 因 Playwright headless bench 由 Cursor track 3 / 后续 track 补）

| metric | 步骤 | 目标 | 实测 |
|---|---|---|---|
| H.1 5 层 fixture 首屏 < 100ms | 切到 5 层 16 leaf fixture · DevTools 录 mount 时间 | < 100ms | <TBD>ms · 判定：<PASS_or_FAIL> |
| H.2 最深层 splitter 拖 P99 < 16ms | DevTools Performance 录 1s drag · 看帧时长 P99 | < 16ms | <TBD>ms · 判定：<PASS_or_FAIL> |
| H.3 preset apply DOM commit < 100ms | 录制 preset 切换 · 看 DOM commit time | < 100ms | <TBD>ms · 判定：<PASS_or_FAIL> |
| H.4 ⌘⌥Arrow navigation < 50ms | 录制 keydown → focus ring 可见 | < 50ms | <TBD>ms · 判定：<PASS_or_FAIL> |
| H.5 maximize enter / exit 各 < 200ms | 已在 evidence 04 测过 | < 200ms 各 | 见 evidence 04 |

### 2.8 收尾 + metrics 文件

```bash
pkill -f "vite\|tauri"
```

写入 `docs/runtime-evidence/mvp-14/phase-d/metrics.md`（仿 MVP-12 格式 · 含 H.1-H.5 + B.2-B.4 + E.1-E.5）

---

## §3 · MVP-15 Phase D capture（30-40 min · 12 evidence + 5 metrics）

### 3.0 evidence 清单总览（mandatory · I5）

视觉回归 baseline：5 主流语言 × 2 主题 = 10 张 + 大文件 2 张 = 12 张

| # | 文件 | acceptance | 用途 |
|---|---|---|---|
| 01-10 | `01-shiki-{lang}-{theme}.png` × 10 | E.1（视觉回归 baseline） | typescript/rust/python/go/java × light/dark |
| 11 | `11-1mb-diff-first-paint.png` | F.1（1MB diff 首屏 < 300ms P99） | DevTools Performance |
| 12 | `12-10mb-streaming-long-task.png` | F.2（10MB long task < 50ms） | DevTools Performance + Worker tab |

### 3.1 准备 fixture（Codex track 1 交付后用）

依赖 Codex track 1 PR merged · 即 `web/tests/fixtures/syntax-highlight/{1mb-typescript.ts, 10mb-typescript.ts, 1mb-{rust,python,go,java}.{rs,py,go,java}}`。

```bash
ls web/tests/fixtures/syntax-highlight/
# 必须有 1mb-typescript.ts / 10mb-typescript.ts / 1mb-rust.rs / 1mb-python.py / 1mb-go.go / 1mb-java.java
# 若缺 · 等 Codex track 1 PR merge 后再跑 §3
```

### 3.2 启动

```bash
pnpm tauri:dev
```

### 3.3 evidence 01-10 · 5 lang × 2 theme baseline

每张：
1. 开 git workspace · 注入 1mb-{lang} fixture · 触发 diff（模拟修改 · `git status` 显示该文件 changed）
2. 切到 Diff panel · 等 shiki 高亮完成（< 300ms）
3. 切 theme 到 light · 截图 → `01-shiki-typescript-light.png`
4. 切 theme 到 dark · 截图 → `02-shiki-typescript-dark.png`
5. 重复 lang：rust / python / go / java · 各 light + dark · 总 10 张

文件命名表：
```
01-shiki-typescript-light.png
02-shiki-typescript-dark.png
03-shiki-rust-light.png
04-shiki-rust-dark.png
05-shiki-python-light.png
06-shiki-python-dark.png
07-shiki-go-light.png
08-shiki-go-dark.png
09-shiki-java-light.png
10-shiki-java-dark.png
```

### 3.4 evidence 11 · 1MB diff 首屏 F.1

1. DevTools Performance 准备录制
2. 关闭 diff panel · 清 LRU cache（重启 app 最稳）
3. 录制 5s · 点击 status 1mb-typescript.ts · 等首屏 DOM commit
4. 测 5 次取 P99 · metrics 填 `F.1 1MB 首屏 P99：<TBD>ms · 判定：<PASS_or_FAIL>`（目标 < 300ms）
5. 截图 Performance 面板 → `11-1mb-diff-first-paint.png`

### 3.5 evidence 12 · 10MB streaming + F.2 long task

1. DevTools Performance 录制 10s
2. 打开 10mb-typescript.ts diff
3. 滚动到底（触发分段加载）
4. 看 long task entries · P99 < 50ms
5. metrics 填 `F.2 10MB long task P99：<TBD>ms · 判定：<PASS_or_FAIL>`
6. 截图 → `12-10mb-streaming-long-task.png`

### 3.6 F.3 theme switch + F.4 LRU + F.5 内存 · DevTools 数字

| metric | 步骤 | 目标 | 实测 |
|---|---|---|---|
| F.3 theme switch | DevTools Performance 录 1MB diff theme 切换 · 测 3 次 P99 | < 50ms | <TBD>ms · 判定：<PASS_or_FAIL> |
| F.4 LRU cache hit | 打开 1mb-typescript.ts 第一次 · 关闭 · 再打开 · 测第二次 P99 | < 5ms | <TBD>ms · 判定：<PASS_or_FAIL> |
| F.5 内存 < 100MB | 连开 10 个 1mb fixture · Chrome DevTools Memory heap snapshot | < 100MB | <TBD>MB · 判定：<PASS_or_FAIL> |

### 3.7 §E.2 WCAG AA contrast audit

1. 在 dark theme + typescript fixture · 打开 Chrome DevTools Lighthouse 面板
2. 跑 "Accessibility" audit · 看 color-contrast 项
3. 若有 token contrast < 4.5:1 · 标 acceptance E.2 FAIL · 创建 issue（不在本 capture 内 fix）
4. metrics 填 `E.2 WCAG AA contrast：<TBD_issues> · 判定：<PASS_or_FAIL>`

### 3.8 metrics + README

写入 `docs/runtime-evidence/mvp-15/phase-d/metrics.md`（仿 MVP-12 格式 · 含 F.1-F.5 + E.2）

---

## §4 · MVP-16 Phase D capture（15-20 min · 4 evidence + spec D 完整勾选）

### 4.0 evidence 清单总览

| # | 文件 | acceptance | 用途 |
|---|---|---|---|
| 01 | `01-rebase-editor-modal.png` | A.x rebase interactive editor | RebaseEditor UI |
| 02 | `02-3way-conflict-view.png` | D.3 + D.4（3-way + 4 action buttons） | 3-way conflict 三栏 |
| 03 | `03-conflict-banner-continue.png` | D.7 + E.x（Continue path） | ConflictBanner 全 resolved 后 Continue 启用 |
| 04 | `04-crash-banner-detect.png` | F.x crash recovery banner | 启动检测 banner |

### 4.1 准备 fixture · 触发 conflict

1. 在 Vibestation 内开 fixture git repo · 触发 rebase conflict（用 spec §C.1 fixture 准备脚本）
2. 启 dev mode · 切到 git rebase 操作

### 4.2 evidence 01 · RebaseEditor modal

1. 触发 rebase interactive · 弹 RebaseEditor modal
2. 截图 → `01-rebase-editor-modal.png`

### 4.3 evidence 02 · 3-way conflict view（D.3 + D.4）

1. rebase 触发 conflict · 跳到 Diff panel
2. 截图三栏 Ours / Base / Theirs · 4 action 按钮（Accept Ours/Theirs/Both/Manual edit）显式可见
3. 截图 → `02-3way-conflict-view.png`

### 4.4 evidence 03 · ConflictBanner Continue 启用

1. 所有 hunk resolved · 文件 Mark as resolved
2. 截图 banner Continue 按钮启用状态 + Abort/Skip 按钮也显式
3. 截图 → `03-conflict-banner-continue.png`

### 4.5 evidence 04 · CrashBanner workspace switch detect

1. 模拟 rebase 进行中 · 强 kill app（`pkill -9 vibestation`）
2. 重启 app · 切到原 workspace
3. 截图 CrashBanner 显示 + 3 按钮（Continue / Abort / Skip / Dismiss）
4. 截图 → `04-crash-banner-detect.png`

### 4.6 Linux 跨平台 deferred

spec §H.6 Linux 跨平台 deferred v0.2 W17 dev VM。本 capture **不做** · 在 metrics 标 explicit skip + 引用 PROGRESS.md L18 deferred 决策。

### 4.7 metrics + README

写入 `docs/runtime-evidence/mvp-16/phase-d/metrics.md`：

```markdown
# MVP-16 Phase D · GUI capture metrics

- baseline HEAD: <TBD>
- 平台: macOS only · Linux 跨平台 deferred v0.2 W17
- Criterion bench：见 [criterion/README.md](../criterion/README.md)（PR #266 · macOS arm64 baseline · 7 bench 8.2×-169.5× 余裕）

## acceptance 判定（GUI）

- A.x RebaseEditor modal：<yes_or_no> · 判定：<PASS_or_FAIL>
- D.3 3-way Diff 三栏：<yes_or_no> · 判定：<PASS_or_FAIL>
- D.4 4 action 按钮显式：<yes_or_no> · 判定：<PASS_or_FAIL>
- D.7 ConflictBanner Continue 启用：<yes_or_no> · 判定：<PASS_or_FAIL>
- F.x CrashBanner detect：<yes_or_no> · 判定：<PASS_or_FAIL>

## 跨平台 skip

- H.6 Linux cross-platform · explicit skip · 留 v0.2 W17 dev VM（PROGRESS.md L18 deferred item）
```

---

## §5 · 汇总 · 4 MVP metrics 完整性自检（mandatory · I5/I9/I12/I13）

跑下面 grep · 任一异常即 BLOCK commit · 修后再跑：

```bash
# I9 + I12 · 拒绝 <TBD> 残留 / <yes_or_no> / <PASS_or_FAIL> 未填
grep -nE "<TBD>|<yes_or_no>|<PASS_or_FAIL>|<degraded_or_full>|<TBD_issues>" docs/runtime-evidence/mvp-1[2-6]/phase-d/metrics.md
# 期望输出：空 · 全部 placeholder 已被 runner 填实值

# I12 · 拒绝 explicit FAIL
grep -nE "判定：FAIL|判定：no" docs/runtime-evidence/mvp-1[2-6]/phase-d/metrics.md
# 期望输出：空 · 若有 FAIL · runner 必须 fix + 重 capture 或开 issue 标 v0.2 deferred

# I13 · 每 acceptance 必有判定字段 · cross-check
for mvp in 12 14 15 16; do
  count=$(grep -cE "判定：(PASS|FAIL)" docs/runtime-evidence/mvp-${mvp}/phase-d/metrics.md 2>/dev/null)
  echo "MVP-${mvp} 判定字段 count: ${count}"
done
# 期望：MVP-12 ≥ 8 · MVP-14 ≥ 10 · MVP-15 ≥ 6 · MVP-16 ≥ 5
```

### 5.2 文件命名 + 体积 + 进 git 校验（用 Cursor track 3 的 validator）

```bash
node scripts/validate-runtime-evidence.mjs --report /tmp/v0.3-sprint-validation.md
# 期望：MVP-12/14/15/16 全 PASS · 不可 ERROR
cat /tmp/v0.3-sprint-validation.md
```

### 5.3 4 MVP spec status flip

跑完所有 grep + validator PASS 后 · 主 agent（不是 Arbiter · 本身权限分离）编辑 4 spec frontmatter：

```bash
# 各 spec status: ready → done
sed -i '' 's/^status: ready$/status: done/' docs/tasks/MVP-12-commit-rail-graph.md
sed -i '' 's/^status: ready$/status: done/' docs/tasks/MVP-14-pane-advanced-layout.md
sed -i '' 's/^status: ready$/status: done/' docs/tasks/MVP-15-diff-syntax-highlight.md
sed -i '' 's/^status: ready$/status: done/' docs/tasks/MVP-16-rebase-merge-cherrypick.md

# 同步 Phase D 行 status `🔄 待实施` → `✅ done · capture batch PR #N`
# 手动编辑 4 spec 的 实施进度表 · 写入 PR # 占位
```

---

## §6 · Commit & PR（mandatory · I5 evidence count 跨段一致）

### 6.1 完整 evidence 清单（必须等于 §1-§4 头表数 · I5 自检）

- MVP-12 phase-d/：8 PNG + metrics.md = 9 文件
- MVP-14 phase-d/：4 PNG + 1 MP4 + metrics.md = 6 文件
- MVP-15 phase-d/：12 PNG + metrics.md = 13 文件
- MVP-16 phase-d/：4 PNG + metrics.md = 5 文件
- 总计：28 PNG + 1 MP4 + 4 metrics.md = 33 文件

### 6.2 commit 拆分建议（分 MVP commit · 4-5 commit）

```bash
git checkout -b chore/v0.3-sprint-phase-d-capture

# commit 1 · MVP-12
git add docs/runtime-evidence/mvp-12/phase-d/
git add docs/tasks/MVP-12-commit-rail-graph.md
git commit -m "docs(MVP-12): Phase D capture · 8 evidence + metrics + spec done"

# commit 2 · MVP-14
git add docs/runtime-evidence/mvp-14/phase-d/
git add docs/tasks/MVP-14-pane-advanced-layout.md
git commit -m "docs(MVP-14): Phase D capture · 5 evidence + metrics + spec done"

# commit 3 · MVP-15
git add docs/runtime-evidence/mvp-15/phase-d/
git add docs/tasks/MVP-15-diff-syntax-highlight.md
git commit -m "docs(MVP-15): Phase D capture · 12 evidence + metrics + spec done"

# commit 4 · MVP-16
git add docs/runtime-evidence/mvp-16/phase-d/
git add docs/tasks/MVP-16-rebase-merge-cherrypick.md
git commit -m "docs(MVP-16): Phase D capture · 4 evidence + metrics + spec done"

# commit 5 · validation report 刷新（Cursor track 3 PR merged 后跑）
node scripts/validate-runtime-evidence.mjs --report docs/runtime-evidence/_VALIDATION-REPORT.md
git add docs/runtime-evidence/_VALIDATION-REPORT.md
git commit -m "docs(runtime-evidence): validation report 刷新 · v0.3 sprint Phase D 后 33 文件全 PASS"

git push -u origin chore/v0.3-sprint-phase-d-capture
gh pr create --base main --title "v0.3 sprint Phase D capture · 4 MVP 28 PNG + 1 MP4 + 4 metrics + 4 spec done flip" --body "<按下方模板>"
```

### 6.3 PR body 模板（v2-D.2 trailer + I5 evidence summary）

```markdown
## §v0.3 sprint Phase D capture 汇总

### evidence count
- MVP-12：8 PNG + metrics.md（acceptance F.1/F.2/F.5/F.6/F.7 + C.4/C.6/C.7）
- MVP-14：4 PNG + 1 MP4 + metrics.md（acceptance B.2/B.3/B.4 + E.1-E.5 + H.1-H.5）
- MVP-15：12 PNG + metrics.md（5 lang × 2 theme baseline + F.1/F.2/F.3/F.4/F.5 + E.2 Lighthouse）
- MVP-16：4 PNG + metrics.md（A.x RebaseEditor + D.3/D.4 + D.7 + F.x CrashBanner）· Linux 跨平台 explicit skip 推 v0.2 W17

### validator
- [x] `node scripts/validate-runtime-evidence.mjs --strict` PASS（Cursor track 3 PR #N merged 后跑）
- [x] 33 文件全合规 R1-R4
- [x] `_VALIDATION-REPORT.md` 刷新

### spec status flip
- [x] MVP-12 spec status: ready → done
- [x] MVP-14 spec status: ready → done
- [x] MVP-15 spec status: ready → done
- [x] MVP-16 spec status: ready → done

### Invariant self-check（I5 / I9 / I12 / I13 · grep 无残留）
- [x] grep `<TBD>` 等 placeholder 空
- [x] grep `判定：FAIL` 空（或显式列出 + 标 v0.2 deferred issue 编号）
- [x] 每 acceptance 项有 `判定：PASS` 字段

## Implemented by · Reviewed by

- Implemented by: tajiaoyezi（Arbiter · 主 capture runner）
- Reviewed by: Claude Code 主 agent · self-review（单人项目 v2-D.2 模式）
- Arbiter approval: tajiaoyezi · YYYY-MM-DD HH:MM · "v0.3 sprint Phase D capture playbook 跑完 · 4 MVP 全 done flip"
```

---

## §7 · 自检 grep（写完 / 修完 playbook 必跑）

```bash
# I2/I4 · 含糊步骤
grep -nE "测 1 次|状态可选|optional|大致" docs/playbooks/v0.3-sprint-capture-playbook.md
# 期望：空

# I9 · placeholder token 唯一 · 不撞 template
grep -nE "<TBD>|<yes_or_no>|<PASS_or_FAIL>|<degraded_or_full>|<TBD_issues>" docs/playbooks/v0.3-sprint-capture-playbook.md
# 期望：仅在 §1-§4 metrics 模板段出现 · 不在叙述段误用

# I5 · 跨段 evidence 数量一致
echo "MVP-12 §1 evidence 数 vs §6 commit 数：8 vs 8 · §6.1 vs 头表"
echo "MVP-14 §2 evidence 数 vs §6 commit 数：5 vs 5"
echo "MVP-15 §3 evidence 数 vs §6 commit 数：12 vs 12"
echo "MVP-16 §4 evidence 数 vs §6 commit 数：4 vs 4"
# 手动 cross-check · 不可漂移

# I13 · 每 MVP metrics 文件结构齐全
for mvp in 12 14 15 16; do
  echo "=== MVP-${mvp} ==="
  grep -E "acceptance|判定" docs/playbooks/v0.3-sprint-capture-playbook.md | grep -c "MVP-${mvp}\|^- ${mvp}"
done
```

---

## §8 · 失败 / 缺失 evidence 处理

如本 capture 任一项 FAIL：

1. **不要在本 PR 强制 PASS** · 真填 FAIL + 引用 issue
2. **开 issue** 标 `v0.2 deferred` label · 链接到本 PR + spec acceptance 项
3. 本 PR 仍可合（部分 done）· 但对应 spec status flip 改为：
   - `status: ready` 保持 + 实施进度表对应 acceptance 留 `🟡 部分 done · 见 issue #N`
   - **不可** flip `done` 当其中含 FAIL
4. 主 agent merge 时把 issue 加到 PROGRESS.md "deferred" 段

---

## §9 · 维护

本 playbook 跑完 1 次后 · 主 agent 在 commit 末尾追加 changelog 注：

```markdown
## changelog
- 2026-MM-DD · v1.0 · 首次跑 · capture batch PR #N · 28 PNG + 1 MP4 + 4 metrics · 全 PASS（或列 FAIL + deferred issue）
- 2026-MM-DD · v1.1 · 若需重 capture · 标变更原因（如 ENABLE_RAIL_GRAPH flag 翻 default true 后 · MVP-12 baseline 失效需重做）
```

---

**本 playbook 由主 agent（Claude Code · session 28 · 2026-05-12）维护 · 4-track 并发 dispatch 后 track 4 交付**。

参考依据：
- MVP-05 playbook 14 invariant（Codex round 1-4 实战验证）
- `.claude/rules/runtime-evidence-location.md` R1-R5 硬规则
- `.claude/rules/dispatch-prompt-template.md` §2.10 evidence-based + §2.14 reviewer dev mode 责任
- 4 spec：MVP-12/14/15/16 Phase D acceptance 段
